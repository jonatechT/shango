/**
 * Clés localStorage partagées entre AuthService et l'intercepteur HTTP.
 * Centralisées ici pour éviter toute divergence entre les deux.
 */
export const TOKEN_KEY = 'shango_token';
export const USER_KEY = 'shango_user';
